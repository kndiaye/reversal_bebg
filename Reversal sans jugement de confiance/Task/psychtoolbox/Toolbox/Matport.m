function [result] = Matport(func, parameters)
    %MATPORT NTPort Library add-on for MATLAB.
    %   Before using Matport, your must install the latest version of NTPort
    %   Library (http://www.zealsoftstudio.com/ntport/).
    %
    %   [result] = Matport(func, parameters) calls the given function
    %   of NTPort Library.
    
end
